mqtt:
  host: mqtt

cameras:
  test:
    ffmpeg:
      inputs:
        - path: /media/streams/wrongway.mp4
          input_args: -re -stream_loop -1 -fflags +genpts
          roles:
            - detect
            - rtmp
    detect:
      enabled: true
      height: 1080
      width: 1920
      fps: 5
version: 0.17-0
